local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "⭐StarBypass⭐",
    Icon = 0,
    LoadingTitle = "StarBypass!",
    LoadingSubtitle = "by Onyx :3",
    Theme = "Bloom",
    DisableRayfieldPrompts = false,
    DisableBuildWarnings = false,
    ConfigurationSaving = {
        Enabled = true,
        FolderName = nil,
        FileName = "Big Hub"
    },
    Discord = {
        Enabled = false,
        Invite = "noinvitelink",
        RememberJoins = true
    },
    KeySystem = true,
    KeySettings = {
        Title = "⭐OnyxBypass⭐",
        Subtitle = "StarBypass Key System",
        Note = "Best bypasser ever :3",
        FileName = "Key",
        SaveKey = true,
        GrabKeyFromSite = false,
        Key = {"Stars"," Star","star","stars","⭐","Onyx","StarBypass"}
    }
})

Rayfield:Notify({
    Title = "StarBypass",
    Content = "Thanks For using StarBypass ⭐",
    Duration = 4,
    Image = 0,
})

local Tab = Window:CreateTab("★Main★"," message-circle-more")
local Section = Tab:CreateSection("⭐StarBypass⭐")

local isToggleOn = false

local charMap = {
    ["a"] = "ạ̣̺", ["A"] = "Ạ̣̺", ["b"] = "ḅ̺", ["B"] = "Ḅ̺", ["c"] = "c̣̺",
    ["C"] = "C̣̺", ["d"] = "ḍ̺", ["D"] = "Ḍ̺", ["e"] = "ẹ̣̺", ["E"] = "Ẹ̣̺",
    ["f"] = "f̣̺", ["F"] = "F̣̺", ["g"] = "ɡ̣̺", ["G"] = "ɢ̣̺", ["h"] = "ḥ̺",
    ["H"] = "Ḥ̺", ["i"] = "ị̣̺", ["I"] = "Ị̣̺", ["j"] = "j̣̺", ["J"] = "J̣̺",
    ["k"] = "ḳ̺", ["K"] = "Ḳ̺", ["l"] = "ḷ̺", ["L"] = "Ḷ̺", ["m"] = "ṃ̺",
    ["M"] = "Ṃ̺", ["n"] = "ṇ̣̺", ["N"] = "Ṇ̣̺", ["o"] = "ọ̣̺", ["O"] = "Ọ̣̺",
    ["p"] = "p̣̺", ["P"] = "P̣̺", ["q"] = "q̣̺", ["Q"] = "Q̣̺", ["r"] = "ṛ̺",
    ["R"] = "Ṛ̺", ["s"] = "ṣ̣̺", ["S"] = "Ṣ̣̺", ["t"] = "ṭ̺", ["T"] = "Ṭ̺",
    ["u"] = "ụ̣̺", ["U"] = "Ụ̣̺", ["v"] = "ṿ̺", ["V"] = "Ṿ̺", ["w"] = "ẉ̺",
    ["W"] = "Ẉ̺", ["x"] = "x̣̺", ["X"] = "X̣̺", ["y"] = "ỵ̺", ["Y"] = "Ỵ̺",
    ["z"] = "ẓ̺", ["Z"] = "Ẓ̺"
}

local function convertMessage(message)
    local converted = ""
    for char in message:gmatch(".") do
        converted = converted .. (charMap[char] or char)
    end
    return converted
end

local Toggle = Tab:CreateToggle({
    Name = "Auto Chat Bypass",
    CurrentValue = false,
    Flag = "Toggle1",
    Callback = function(Value)
        isToggleOn = Value
        Rayfield:Notify({
            Title = "⭐StarBypass⭐",
            Content = isToggleOn and "Auto Chat Bypass Enabled!" or "Auto Chat Bypass Disabled!",
            Duration = 5
        })
    end,
})

local player = game.Players.LocalPlayer
local DefaultChatSystem = game.ReplicatedStorage:WaitForChild("DefaultChatSystemChatEvents", 10)
local SayMessageRequest = DefaultChatSystem and DefaultChatSystem:WaitForChild("SayMessageRequest", 10)

if SayMessageRequest then
    player.Chatted:Connect(function(message)
        if isToggleOn then
            local convertedMessage = convertMessage(message)
            SayMessageRequest:FireServer(convertedMessage, "All")
        end
    end)
end

local TextChatService = game:GetService("TextChatService")
if TextChatService then
    local function onLocalMessage(message)
        if isToggleOn and message.TextSource == player then
            local convertedMessage = convertMessage(message.Text)
            SayMessageRequest:FireServer(convertedMessage, "All")
            return nil
        end
        return message
    end
    TextChatService.OnIncomingMessage = onLocalMessage
end

local Input = Tab:CreateInput({
    Name = "Manual Chat Bypass",
    CurrentValue = "",
    PlaceholderText = "Input Text",
    RemoveTextAfterFocusLost = false,
    Flag = "Input1",
    Callback = function(Text)
        if Text and Text ~= "" then
            local convertedMessage = convertMessage(Text)
            SayMessageRequest:FireServer(convertedMessage, "All")
        end
    end,
})

local Button = Tab:CreateButton({
    Name = "Fix chat filter",
    Callback = function()
        local premadeMessage = "...-..-.-.-...-."
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "Starbypasser is the fucking best",
    Callback = function()
        local premadeMessage = "Starbypasser is the fucking best"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "ONYXTHESTARR made this shit your goofy ass bypass so ass bro💀",
    Callback = function()
        local premadeMessage = "ONYXTHESTARR made this shit your goofy ass bypass so ass bro💀"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "alr alr now shut the fuck up Onyxthestar on top",
    Callback = function()
        local premadeMessage = "alr alr now shut the fuck up Onyxthestar on top"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Tab = Window:CreateTab("Information", "info")
local Label = Tab:CreateLabel("⭐StarBypass⭐ StarBypass is a tool made by ONYXTHESTARR designed to bypass the chat on roblox and speak your mind with a cleaner method and with a new look and new ui!", "rewind")

local Tab = Window:CreateTab("Premade bypasses", "message-square-code")
local Button = Tab:CreateButton({
    Name = "Fuck you nigga im better lmfao",
    Callback = function()
        local premadeMessage = "Fuck you nigga im better lmfao"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "suck my dick nigga",
    Callback = function()
        local premadeMessage = "suck my dick nigga"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "Bitch shut the fuck up",
    Callback = function()
        local premadeMessage = "Bitch shut the fuck up"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "Fuck you bro",
    Callback = function()
        local premadeMessage = "Fuck you bro"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "yeah yeah says the one whos a fucking retard",
    Callback = function()
        local premadeMessage = "yeah yeah says the one whos a fucking retard"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "ok keep talking nigga",
    Callback = function()
        local premadeMessage = "ok keep talking nigga"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "go die in a fucking ditch",
    Callback = function()
        local premadeMessage = "go die in a fucking ditch"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Tab = Window:CreateTab("Sex","banana")

local Button = Tab:CreateButton({
    Name = "Oh yeah *pounds harder* take that dick bitch",
    Callback = function()
        local premadeMessage = "Oh yeah *pounds harder* take that dick bitch"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "let me see your ass",
    Callback = function()
        local premadeMessage = "let me see your ass"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "*put my dick inside* you like that cock dont you",
    Callback = function()
        local premadeMessage = "*puts my dick inside* you like my cock dont you"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "*cums all over you*",
    Callback = function()
        local premadeMessage = "*cums all over you*"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "gay section: 
*grabs your cock*",
    Callback = function()
        local premadeMessage = "*grabs your cock*"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "your dick is so small...",
    Callback = function()
        local premadeMessage = "your dick is so small..."
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})

local Button = Tab:CreateButton({
    Name = "i love your tight asshole",
    Callback = function()
        local premadeMessage = "i love your tight asshole"
        local convertedMessage = convertMessage(premadeMessage)
        SayMessageRequest:FireServer(convertedMessage, "All")
    end,
})
